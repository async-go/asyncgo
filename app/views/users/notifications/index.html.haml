%h1.visually-hidden Notifications

%ul
  - if unique_unread_notifications.present?
    - unique_unread_notifications.each do |notification|
      - puts notification.inspect
      %li.mt-3= link_to notification_text(notification),
        user_notifications_path(current_user.id, notification)
  - else
    No notifications
